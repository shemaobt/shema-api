# The model ladder and its parameters, as the room chooses them today.
#
# DOCTRINE.md §5.1 — prompts/*.md, the model ladder and its parameters are Marcia's artifacts:
# any change is a ruling with her word, never an engineering default.
#
# Three columns, two spaces apart: where the value is chosen, the value, and the ruling that
# put it there. `unruled` is an inherited default, frozen at what it is today rather than
# credited to a ruling she never made — that distinction is the point of the file. A row is
# keyed by the function the call sits in, never by a line: eighteen tickets touch one file.
#
# Read out of the tree by `scripts/sync_doctrine.py`; `--check` fails when the two disagree.

app/core/config.py::tripod_voice_model  claude-fable-5-1,claude-opus-5,claude-opus-4-8  2026-09-03-the-model-seam-is-hers
app/core/config.py::tripod_analysis_model  claude-fable-5-1,claude-opus-5,claude-opus-4-8  2026-09-03-the-model-seam-is-hers
app/core/config.py::tripod_classifier_model  claude-sonnet-5,claude-sonnet-4-6  2026-09-03-the-model-seam-is-hers
app/services/internalization_room/back_translation.py::analyse_telling_back  ladder=analysis_ladder(cfg) max_output_tokens=4096 effort='high' thinks=True  unruled
app/services/internalization_room/back_translation.py::verify_correction  ladder=analysis_ladder(cfg) max_output_tokens=4096 effort='high' thinks=True  unruled
app/services/internalization_room/classify_coverage.py::classify_coverage  ladder=classifier_ladder(cfg) max_output_tokens=4096 effort='high' thinks=False  unruled
app/services/internalization_room/validated_turn.py::_draft  ladder=voice_ladder(settings) max_output_tokens=4096 effort='high' thinks=True  unruled
app/services/internalization_room/validated_turn.py::_voiced_after_validation  ladder=voice_ladder(settings) max_output_tokens=4096 effort='high' thinks=True  unruled
